{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opennfr.io/schema/v1/requirementset.schema.json",
  "title": "RequirementSet",
  "description": "The container a performance requirement is written into. Deliberately minimal: it fixes the shape and nothing else. Metric and attribute names are plain strings, borrowed from OpenTelemetry rather than defined here, so measuring something new never means changing the format.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "properties": {
    "apiVersion": {
      "const": "opennfr.io/v1"
    },
    "kind": {
      "const": "RequirementSet"
    },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "$ref": "#/$defs/name"
        },
        "annotations": {
          "$ref": "#/$defs/annotations"
        },
        "displayName": {
          "$ref": "#/$defs/displayName"
        }
      }
    },
    "spec": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "requirements"
      ],
      "properties": {
        "requirements": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/requirement"
          }
        }
      }
    }
  },
  "$defs": {
    "indicator": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "selector"
      ],
      "description": "What the requirement is about. `selector` says which requests; `metric` says what to measure of them, and is omitted when the requirement is about the requests themselves rather than a quantity they carry. There is no second shape: whether you get a value, a count or a share is decided by the aggregation, which is where it belongs.",
      "properties": {
        "selector": {
          "$ref": "#/$defs/selector"
        },
        "metric": {
          "type": "string",
          "minLength": 1,
          "description": "A metric name, borrowed from OpenTelemetry semantic conventions where one exists. This schema does not enumerate metric names and never will: enumerating them would make every new metric a change to the format. Omit it when nothing is measured — an error share counts requests, and naming a metric it never reads is how a document ends up saying `duration` in a requirement about failures."
        }
      },
      "examples": [
        {
          "metric": "http.client.request.duration",
          "selector": {
            "loadtest.request.name": "GET /"
          }
        },
        {
          "selector": {}
        }
      ]
    },
    "predicate": {
      "type": "object",
      "required": [
        "aggregation",
        "op",
        "threshold",
        "unit"
      ],
      "properties": {
        "name": {
          "$ref": "#/$defs/name",
          "description": "Optional. Identifies this predicate in a report; without it the aggregation is the identifier, so two predicates in one requirement may not then share an aggregation."
        },
        "aggregation": {
          "$ref": "#/$defs/aggregation"
        },
        "of": {
          "$ref": "#/$defs/selector",
          "description": "Narrows the indicator's selection to the requests this predicate counts — the failed ones, typically. Applied on top of the indicator's selector, never instead of it. Required by `share`, allowed with `count`, meaningless elsewhere: a percentile of a subset is a percentile of a different indicator."
        },
        "op": {
          "$ref": "#/$defs/op"
        },
        "threshold": {
          "type": "number"
        },
        "unit": {
          "$ref": "#/$defs/unit"
        },
        "displayName": {
          "$ref": "#/$defs/displayName"
        }
      },
      "examples": [
        {
          "aggregation": "p95",
          "op": "lte",
          "threshold": 500,
          "unit": "ms"
        },
        {
          "aggregation": "share",
          "of": {
            "error.type": "*"
          },
          "op": "lte",
          "threshold": 5,
          "unit": "%"
        },
        {
          "aggregation": "count",
          "of": {
            "error.type": "*"
          },
          "op": "lte",
          "threshold": 100,
          "unit": "{request}"
        },
        {
          "name": "reached-target-rate",
          "aggregation": "rate",
          "op": "gte",
          "threshold": 200,
          "unit": "{request}/s"
        }
      ],
      "allOf": [
        {
          "if": {
            "properties": {
              "aggregation": {
                "const": "share"
              }
            },
            "required": [
              "aggregation"
            ]
          },
          "then": {
            "required": [
              "of"
            ]
          }
        },
        {
          "if": {
            "not": {
              "properties": {
                "aggregation": {
                  "enum": [
                    "count",
                    "share"
                  ]
                }
              },
              "required": [
                "aggregation"
              ]
            }
          },
          "then": {
            "not": {
              "required": [
                "of"
              ]
            }
          }
        }
      ]
    },
    "requirement": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "indicator",
        "criteria"
      ],
      "properties": {
        "name": {
          "$ref": "#/$defs/name"
        },
        "indicator": {
          "$ref": "#/$defs/indicator"
        },
        "guards": {
          "type": "array",
          "minItems": 1,
          "description": "Preconditions: statements that the run happened in the intended regime at all. Same shape as a criterion, and the difference is what a violation means — a criterion says the system did not hold, a guard says the run did not happen as intended, so a green criterion beside a violated guard is not evidence of anything. Optional, and a requirement without one asserts nothing about the conditions it assumes.",
          "items": {
            "allOf": [
              {
                "$ref": "#/$defs/predicate"
              }
            ],
            "properties": {
              "indicator": {
                "$ref": "#/$defs/indicator"
              }
            },
            "unevaluatedProperties": false
          }
        },
        "criteria": {
          "type": "array",
          "minItems": 1,
          "items": {
            "allOf": [
              {
                "$ref": "#/$defs/predicate"
              }
            ],
            "unevaluatedProperties": false
          }
        },
        "displayName": {
          "$ref": "#/$defs/displayName"
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "indicator": {
                "required": [
                  "ratio"
                ]
              }
            },
            "required": [
              "indicator"
            ]
          },
          "then": {
            "properties": {
              "criteria": {
                "items": {
                  "properties": {
                    "aggregation": {
                      "enum": [
                        "rate",
                        "count"
                      ]
                    }
                  }
                }
              }
            }
          },
          "description": "docs/GLOSSARY.md permits only rate and count over a ratio. A percentile of a fraction has no meaning, and nothing downstream could evaluate it."
        }
      ],
      "examples": [
        {
          "name": "checkout-latency-under-load",
          "displayName": "Checkout stays fast at the intended rate",
          "indicator": {
            "metric": "http.client.request.duration",
            "selector": {
              "loadtest.request.name": "POST /checkout"
            }
          },
          "guards": [
            {
              "name": "reached-target-rate",
              "aggregation": "rate",
              "op": "gte",
              "threshold": 200,
              "unit": "{request}/s"
            }
          ],
          "criteria": [
            {
              "name": "tail",
              "aggregation": "p95",
              "op": "lte",
              "threshold": 500,
              "unit": "ms"
            }
          ]
        }
      ]
    },
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
      "maxLength": 253
    },
    "unit": {
      "enum": [
        "ns",
        "us",
        "ms",
        "s",
        "min",
        "h",
        "%",
        "1",
        "By",
        "KiBy",
        "MiBy",
        "GiBy",
        "{request}",
        "{request}/s",
        "{iteration}",
        "{iteration}/s",
        "{vu}"
      ],
      "description": "Closed list, from docs/units.md. ADR-0002 D15 chose an enumeration over full UCUM precisely so a typo like `mss` is a parse error rather than a disagreement between consumers."
    },
    "op": {
      "enum": [
        "lt",
        "lte",
        "gt",
        "gte",
        "eq",
        "neq"
      ]
    },
    "aggregation": {
      "type": "string",
      "anyOf": [
        {
          "enum": [
            "min",
            "max",
            "avg",
            "stddev",
            "sum",
            "count",
            "rate",
            "share"
          ]
        },
        {
          "pattern": "^p\\d{1,2}(\\.\\d+)?$"
        }
      ],
      "description": "How the selected requests are reduced to one number. Two families, and which one you are in is decided by whether the indicator names a metric.\n  Over a metric's VALUES: p50/p95/p99/p99.9 and any other percentile, min, max, avg, stddev, sum.\n  Over the REQUESTS themselves: count (how many), rate (how many per second), share (what fraction of them — needs `of`).\n`rate` is per second and nothing else; the fraction is `share`, because one word meaning both is a word that means neither."
    },
    "selector": {
      "type": "object",
      "description": "Selects time series by attribute. {} means every series. A value of \"*\" means the attribute is present with any value. Attribute names are not enumerated — they are borrowed from OpenTelemetry, whose values may be strings, numbers or booleans.",
      "examples": [
        {},
        {
          "loadtest.request.name": "GET /"
        },
        {
          "loadtest.group.name": "MyGroup",
          "loadtest.request.name": "MyRequest"
        }
      ],
      "additionalProperties": {
        "type": [
          "string",
          "number",
          "boolean"
        ]
      }
    },
    "annotations": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "The only extension point in the format. The opennfr.io/ prefix is reserved."
    },
    "displayName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Free human-readable text beside the machine identifier, in any script. `name` is restricted so something can point at it; this is for a person to read. It is inert: nothing selected, measured or compared depends on it, and two documents differing only in their display names say the same thing. Keep it to the quantity, not the answer — a name reading '99th percentile under 500 ms' beside threshold: 500 is a second source for one number, and the two diverge the first time the threshold moves."
    }
  }
}
